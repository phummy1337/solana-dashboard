__pycache__/
*.pyc
.DS_Store
.env
refresh.log
solana-recap-*.png

# Built at deploy time and published as a Pages artifact, never committed —
# it holds licensed feeds and every commit would archive another snapshot.
data.json

# Captures of DefiLlama's front end, for the same reason: this repo is public,
# and committing them would leave a permanently downloadable copy of their
# dataset in git history. tools/llama_local.py writes them; the build reads
# them from outside the repo.
local/
